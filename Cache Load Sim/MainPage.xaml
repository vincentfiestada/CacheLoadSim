<Page
    x:Class="Cache_Load_Sim.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cache_Load_Sim"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Page.Resources>
        <Style x:Key="textblockstyle1" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22" />
            <Setter Property="Margin" Value="10,10,10,10" />
        </Style>
    </Page.Resources>

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="simulationStats" Style="{StaticResource textblockstyle1}" />
        <TextBlock x:Name="totalHitsResult" Style="{StaticResource textblockstyle1}" />
        <TextBlock x:Name="totalMissesResult" Style="{StaticResource textblockstyle1}" />
        <TextBlock x:Name="totalHitTimeResult" Style="{StaticResource textblockstyle1}" />
        <TextBlock x:Name="totalMissPenaltyResult" Style="{StaticResource textblockstyle1}" />
        <TextBlock x:Name="finalState" Style="{StaticResource textblockstyle1}" />
    </StackPanel>
</Page>